# Helpers Reference

This page will document Terraform expression helpers.

## Helpers

- `tf.var("name")`
- `tf.local("name")`
- `tf.raw("...")`
- `tf.block({ ... })`

## Planned Content

- When a value should be a Terraform expression instead of a string.
- How to represent variables and locals.
- How to emit raw expressions intentionally.
- How to force nested block syntax.

Phase 2 will add examples and guidance.
