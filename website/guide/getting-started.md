# Getting Started

This page will become the shortest path from installation to a generated Terraform file.

## Planned Content

- Install `react-hcl` from npm.
- Run `react-hcl init`.
- Create a minimal `main.tsx`.
- Generate `main.tf`.
- Review the generated HCL.
- Run Terraform validation outside `react-hcl`.

## Placeholder Flow

```bash
npm install -g react-hcl
react-hcl init
react-hcl generate main.tsx -o main.tf
terraform validate
```

Phase 2 will replace this placeholder with a runnable example.
